{
  "name": "@stokk/db",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "prisma generate && tsc -p tsconfig.build.json",
    "clean": "rm -rf dist .turbo src/generated *.tsbuildinfo",
    "generate": "prisma generate",
    "lint": "eslint .",
    "migrate": "prisma migrate dev",
    "reset": "prisma migrate reset --force && tsx scripts/setup-app-role.ts && prisma db seed",
    "seed": "tsx prisma/seed.ts",
    "setup-app-role": "tsx scripts/setup-app-role.ts",
    "studio": "prisma studio --port 5555 --browser none",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "validate": "prisma validate"
  },
  "dependencies": {
    "@prisma/adapter-pg": "catalog:",
    "@prisma/client": "catalog:",
    "@stokk/types": "workspace:*",
    "pg": "catalog:"
  },
  "devDependencies": {
    "@stokk/config": "workspace:*",
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "dotenv": "catalog:",
    "eslint": "catalog:",
    "prisma": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
